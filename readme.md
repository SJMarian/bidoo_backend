# Auction Project

## 📌 Requirements

Before running this project, make sure your system meets the following requirements:

### 🔹 Java

* **Java Version:** 17
* Ensure Java 17 is installed and configured in your system.

You can verify using:

```
java -version
```

---

### 🔹 Spring Boot

* **Spring Boot Version:** 4.0.3

This project is built using Spring Boot **4.0.3**, so make sure your environment supports this version.

---

### 🔹 Database

* **PostgreSQL**

This project is required postgreSQL database. with database name **diboo_db**

---

## ⚙️ Setup Instructions

1. Clone the repository:

```
git clone <your-repository-url>
```

2. Navigate to the project folder:

```
cd <project-folder>
```

3. Build the project:

```
./mvnw clean install
```

or

```
mvn clean install
```

4. Run the application:

```
./mvnw spring-boot:run
```

or

```
mvn spring-boot:run
```

---

## 🚀 Notes

* Ensure the correct Java version is configured in your IDE.
* Update your environment variables if needed.
* If you face any version conflict, double-check your Java and Spring Boot setup.